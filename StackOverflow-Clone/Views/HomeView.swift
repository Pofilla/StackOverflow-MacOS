import SwiftUI

struct HomeView: View {
    var body: some View {
        Text("Home View is empty.")
            .font(.title)
            .padding()
    }
}

// Preview
#Preview {
    HomeView()
} 
